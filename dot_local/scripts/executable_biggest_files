#!/usr/bin/env bash
# description: List the 10 biggest files under a directory (default: current).

DIR=${1:-.}

echo "🔎 Searching for the biggest files in: $DIR"
echo "--------------------------------------------"

find "$DIR" -type f -exec du -h {} + 2>/dev/null | sort -rh | head -n 10
